{
  "project": "Engine Character and String System Planning",
  "planning_only": true,
  "goal": "Introduce one engine-owned character and string layer that describes platform-native character types, explicit encoding types, allocator-aware owning strings, and non-owning string views without relying on default char semantics for wchar or platform text logic.",
  "source_analyzed": [
    "source/runtime/core/include/misc/CompileTimeString.h",
    "source/runtime/core/include/misc/STL.h",
    "source/runtime/core/include/misc/MMemory.h",
    "source/runtime/core/CMakeLists.txt",
    "testscripts/README.md",
    "testscripts/linux/test_taskgraph.sh",
    "testscripts/windows/test_all.ps1"
  ],
  "execute_order": [
    "1",
    "2",
    "3",
    "4",
    "5",
    "6"
  ],
  "checkpoints": [
    {
      "id": "1",
      "title": "Define explicit character taxonomy",
      "must_be_true_after": [
        "Engine text code no longer treats plain `char` as the implicit answer for all platform or wide-character paths.",
        "A single core header defines `Char8`, `Char16`, `Char32`, `WideChar`, `PlatformChar`, and narrow/UTF aliases with compile-time platform selection.",
        "The aliases describe storage units only; they do not imply transcoding, locale conversion, or file-system semantics by themselves.",
        "Platform-native text is named separately from UTF text so Windows wide APIs and non-Windows UTF-8 paths are not mixed accidentally."
      ],
      "files_likely_affected": [
        "source/runtime/core/include/string/CharTypes.h",
        "source/runtime/core/include/misc/CompileTimeString.h",
        "source/runtime/core/include/misc/STL.h"
      ]
    },
    {
      "id": "2",
      "title": "Introduce allocator-aware engine string aliases",
      "must_be_true_after": [
        "The first implementation uses `std::basic_string` with `MoerStlAllocator` instead of creating a fake wrapper around the STL container.",
        "`BasicString<CharT, Traits, Allocator>` and `BasicStringView<CharT, Traits>` exist in the engine namespace with concrete aliases for `String`, `StringView`, `WideString`, `WideStringView`, `PlatformString`, and `PlatformStringView`.",
        "Owning strings default to the engine allocator; views remain allocator-free and non-owning.",
        "The public API exposes direct STL-compatible behavior through aliases, not pass-through member functions or adapter classes."
      ],
      "files_likely_affected": [
        "source/runtime/core/include/string/String.h",
        "source/runtime/core/include/misc/STL.h",
        "source/runtime/core/include/misc/MMemory.h"
      ]
    },
    {
      "id": "3",
      "title": "Make compile-time strings encoding-aware",
      "must_be_true_after": [
        "`CompileTimeString` remains a small compile-time storage utility but is moved or included through the new string boundary.",
        "Compile-time string deduction works for supported character storage types without hard-coding narrow `char` behavior.",
        "The utility does not perform transcoding; it only preserves literal character type and size.",
        "Any new helper exists only if it protects compile-time literal invariants instead of renaming `std::basic_string_view`."
      ],
      "files_likely_affected": [
        "source/runtime/core/include/misc/CompileTimeString.h",
        "source/runtime/core/include/string/CompileTimeString.h",
        "source/runtime/core/include/string/String.h"
      ]
    },
    {
      "id": "4",
      "title": "Add explicit conversion and platform boundary rules",
      "must_be_true_after": [
        "No implicit conversion exists between UTF, wide, and platform strings.",
        "Any required conversion API is named by source and destination encoding, for example UTF-8 to platform and platform to UTF-8.",
        "Conversion functions return engine-owned strings using `MoerStlAllocator` and accept string views at the boundary.",
        "Unsupported or invalid input policy is explicit before implementation: reject invalid sequences or replace them, but never silently reinterpret bytes as characters."
      ],
      "files_likely_affected": [
        "source/runtime/core/include/string/StringConvert.h",
        "source/runtime/core/source/string/StringConvert.cpp",
        "source/runtime/core/CMakeLists.txt"
      ]
    },
    {
      "id": "5",
      "title": "Migrate core aliases without compatibility shims",
      "must_be_true_after": [
        "`misc/STL.h` includes the new string header and exposes engine string aliases from one canonical location.",
        "New runtime/editor code uses engine string names when ownership or platform text semantics matter.",
        "Existing `std::string` usage is migrated only where it crosses engine-owned APIs, allocator-sensitive storage, platform API boundaries, or shared public headers.",
        "No parallel legacy typedefs, deprecated aliases, or compatibility wrappers are kept after migration."
      ],
      "files_likely_affected": [
        "source/runtime/core/include/misc/STL.h",
        "source/runtime/core/include/string/String.h",
        "source/runtime/core/include/string/CharTypes.h",
        "source/runtime/core/include/platform/*"
      ]
    },
    {
      "id": "6",
      "title": "Validate with existing repository scripts only",
      "must_be_true_after": [
        "Header-only alias changes compile through the existing core/editor build instead of a new standalone test harness.",
        "If conversion code is added, it is covered through existing test targets or existing repository test scripts, not an ad-hoc script.",
        "Validation starts with `just b` when available; otherwise use the repository CMake build path already documented for the project.",
        "On Linux, use existing `testscripts/linux/test_taskgraph.sh` only when the touched code affects that tested area; on Windows, use `testscripts/windows/test_all.ps1` for the broader existing suite."
      ],
      "files_likely_affected": [
        "source/runtime/core/CMakeLists.txt",
        "testscripts/README.md",
        "testscripts/linux/test_taskgraph.sh",
        "testscripts/windows/test_all.ps1"
      ]
    }
  ],
  "design_notes": {
    "canonical_headers": "Use `source/runtime/core/include/string/CharTypes.h` for character storage aliases and `source/runtime/core/include/string/String.h` for owning/view string aliases. Keep `misc/STL.h` as an aggregate include only.",
    "preferred_first_version": "Use aliases over `std::basic_string` and `std::basic_string_view` with `MoerStlAllocator`. Do not implement a custom string container until the engine has a concrete invariant that STL cannot express.",
    "platform_character_rule": "`PlatformChar` is `wchar_t` on Windows and UTF-8 storage on non-Windows targets. Treat it as platform API boundary text, not as the engine's universal internal encoding.",
    "internal_encoding_rule": "Prefer UTF-8 engine `String` for persisted data, logs, configs, asset paths after normalization, and editor text unless a platform API requires `PlatformString`.",
    "conversion_rule": "Conversions must be explicit and named. Do not add constructors or overloads that silently convert between `String`, `WideString`, and `PlatformString`.",
    "allocator_rule": "Owning engine strings use `MoerStlAllocator` by default. `StringView` aliases never own memory and must not outlive the storage they reference.",
    "rejection_rule": "Do not add a wrapper class that only forwards STL methods. Add a real class only when it owns a stable invariant such as small-buffer policy, normalized encoding, or allocator state that aliases cannot enforce."
  },
  "validation": [
    "Planning only in this checkpoint. No implementation is part of this file.",
    "This checkpoint update changes documentation JSON only; no build or runtime validation is required for engine behavior.",
    "When implementation starts, run the existing project build first: `just b` if available, otherwise the existing CMake build command for the configured tree.",
    "After implementation, run only existing repository scripts that cover the touched area; do not add one-off string validation scripts outside the test structure."
  ]
}
